/**
 * The version this package reports when it registers with the server.
 *
 * The core publishes on its own cycle, so an application running this adapter
 * has to report this version rather than the core's. Kept in step with
 * package.json by a test rather than by discipline.
 */
export const SDK_VERSION = "0.2.0";
